const electronico = {
  color: "negro",
  nombre: "Monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(Object.keys(electronico)); //para saber si el objeto tiene información

console.log(Object.values(electronico)); //para saber que informacion tiene cada valor del objeto

console.log(Object.entries(electronico)); //te retorna todo en pares
