//solo se utiliza una vez ese id por documento (debe de ser único)
const id = document.getElementById('formulario')
console.log(id);

const noExiste = document.getElementById('no existe')
console.log(noExiste);

