//retorna todos los elementos que concuerder con el selector
const selectAll = document.querySelectorAll('.card')
console.log(selectAll);

const noExiste =  document.querySelectorAll('no existe')
console.log(noExiste);